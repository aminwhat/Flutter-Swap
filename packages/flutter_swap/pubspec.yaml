name: flutter_swap
description: "A new Flutter package project."
version: 0.0.2-dev
homepage: https://github.com/Flutter-Swap/dart

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  rfw: ^1.0.26

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  # uses-material-design: true
